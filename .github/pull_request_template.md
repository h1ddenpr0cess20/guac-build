<!--
Guac Build is a downstream fork of xai-org/grok-build. Internal `xai-*` crate
names are intentionally retained to keep upstream rebases reviewable — see
docs/META_MODEL_API.md for the compatibility boundary.
-->

## Summary

<!-- What does this change do, and why? -->

## Changes

<!-- Bullet the notable changes. Call out anything touching the provider,
     default model, auth, or the compatibility boundary. -->

-

## Testing

<!-- How did you verify this? Prefer per-crate commands; full-workspace builds
     are slow. -->

- [ ] `cargo fmt --all --check`
- [ ] `cargo clippy -p <crate>`
- [ ] `cargo test -p <crate>`
- [ ] Built the binary: `cargo build -p xai-grok-pager-bin --bin guac`

## Notes

<!-- Anything reviewers should know: follow-ups, known gaps, upstream-rebase
     implications. Delete if not applicable. -->
